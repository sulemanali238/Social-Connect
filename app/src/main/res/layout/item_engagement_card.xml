<?xml version="1.0" encoding="utf-8"?>
<!-- item_engagement_card.xml — Likes / Comments / Shares / Saves -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_engagement_card"
    android:padding="12dp">

    <TextView
        android:id="@+id/tvEngValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="-"
        android:textColor="#1E293B"
        android:textSize="20sp"
        android:fontFamily="@font/poppins_bold"/>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="4dp">

        <ImageView
            android:id="@+id/ivEngIcon"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:src="@drawable/ic_insight"/>

        <TextView
            android:id="@+id/tvEngLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Insights..."
            android:textColor="#9CA3AF"
            android:textSize="11sp"
            android:fontFamily="@font/poppins_regular"
            android:layout_marginStart="4dp"/>

    </LinearLayout>

</LinearLayout>